apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: sample-app
  labels:
    app: sample-app
spec:
  ports:
    - name: http
      port: 80
      targetPort: 4180
  selector:
    app: sample-app
